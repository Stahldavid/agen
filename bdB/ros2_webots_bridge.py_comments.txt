The current version of the script is a basic Webots-ROS2 interface. However, it lacks a number of important features that are needed for a more comprehensive interface, such as sensor management, motor commands, and controller management. In the proposed code, the Webots-ROS2 interface is extended to include such additional features, which are implemented by making use of the ros2_control package.